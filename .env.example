# Match this URL to the PORT used by backend-practice/app.
# It is used for auth and the authenticated resume API.
PORT=3008
REACT_APP_BASE_API=http://localhost:3000
# Set true only when you want to preview pagination with 10 local sample resumes.
REACT_APP_USE_MOCK_RESUMES=false
